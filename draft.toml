[environments]
  [environments.development]
    name = "goland_expedition"
    namespace = "default"
    wait = true
    watch = false
    watch-delay = 2
    auto-connect = false
    dockerfile = "Dockerfile"
    chart = ""
    registry = "marianferenc"
